package edu.neu.madcourse.binbo;

import android.content.Context;

public class PBPuzzleView extends BogglePuzzleView {
	private PBGame mGame = null;
	
	public PBPuzzleView(Context context) {

		super(context);
		mGame = (PBGame)context;
		setFocusable(true);
		setFocusableInTouchMode(true);
		
		// ...
		setId(ID);			
	}
}
